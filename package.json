{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "gh-pages -d dist",
    "all-in-one": "nuxt build && nuxt generate && gh-pages -d dist"
  },
  "devDependencies": {
    "@nuxt/content": "^2.11.0",
    "gh-pages": "^6.1.1",
    "nuxt": "^3.9.3"
  }
}
